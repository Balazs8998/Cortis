services:
  postgres:
    image: postgres:16
    container_name: cortis_postgres
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - cortis_postgres_data:/var/lib/postgresql/data

volumes:
  cortis_postgres_data: